<h5>
	<i>Posted <abbr class="timeago" title="<%= comment.escape("updated_at") %>"></abbr> by <%= comment.escape("author_email") %></i>
</h5>
<h4><strong><%= comment.escape("title") %></strong></h4>
<% if (comment.escape("author_id") === StreetEasyClone.currentUser) {%>
	<button class="delete-comment">Delete Comment</button>
<% } %>
<p><%= comment.escape("body") %></p>